# models-and-pitches References

Financial modeling skills (3-statement, DCF, LBO, comps, audit-xls) reference the XBRL calculation linkbase and presentation trees at runtime via `get_statement_structure` and `get_calculation_tree` (, ). Static reference materials are not required for this vertical — models derive their structure from the live `gold.xbrl_presentation` (~8M rows) and `gold.xbrl_calculations` (756K rows) tables.
