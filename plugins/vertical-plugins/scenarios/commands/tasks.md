---
description: agentii.tasks — the spec 046 kit command. Use the Skill tool to run agentii:tasks on this workspace.
---
Use the Skill tool to run `agentii:tasks` on this workspace (spec 046 kit skill — read its SKILL.md for the full methodology, gates and invocation).
