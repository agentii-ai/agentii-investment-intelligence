---
description: agentii.implement — the spec 046 kit command. Use the Skill tool to run agentii:implement on this workspace.
---
Use the Skill tool to run `agentii:implement` on this workspace (spec 046 kit skill — read its SKILL.md for the full methodology, gates and invocation).
